// import { registerAs } from '@nestjs/config';

// export default registerAs('minio', () => ({
// 	endPoint: process.env.STOREIO_ENDPOINT,
// 	port: process.env.STOREIO_USE_SSL === 'true' ? 443 : 80,
// 	useSSL: process.env.STOREIO_USE_SSL === 'true',
// 	accessKey:process.env.STOREIO_ACCESS_KEY,
// 	secretKey:process.env.STOREIO_SECRET_KEY,
// })) 
